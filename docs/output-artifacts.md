# Output Artifacts

All outputs are written to `runs/<run_id>/`. The default run directory is `runs/adele_run/`.

## Directory Layout

```
runs/<run_id>/
  outputs.jsonl            ← Inference results (append-only)
  judge_outputs.jsonl      ← Judge evaluations (append-only)
  merged_results.parquet   ← Combined view (regenerated)
  run_manifest.json        ← Run provenance (updated in-place)
  metrics.json             ← Summary statistics (regenerated)
  ground_truths.json       ← Cached ground truths (written once)
```

## `outputs.jsonl`

One JSON object per line. Each line is an `InferenceOutput` record.

| Field | Type | Description |
|---|---|---|
| `instance_id` | `str` | Dataset instance identifier |
| `model_id` | `str` | Model name or deployment used |
| `prompt` | `str` | The prompt sent to the model |
| `response` | `str` | Model's response text |
| `tokens_prompt` | `int \| null` | Prompt token count (if reported by API) |
| `tokens_completion` | `int \| null` | Completion token count |
| `latency_s` | `float \| null` | Request latency in seconds |
| `finish_reason` | `str \| null` | API finish reason (`stop`, `length`, etc.) |
| `timestamp` | `datetime` | When the response was recorded (UTC) |
| `run_id` | `str` | Run identifier |

**Example line:**

```json
{"instance_id": "abc123", "model_id": "gpt-4o", "prompt": "What is 2+2?", "response": "4", "tokens_prompt": 12, "tokens_completion": 1, "latency_s": 0.45, "finish_reason": "stop", "timestamp": "2025-01-15T10:30:00", "run_id": "adele_dev_run"}
```

## `judge_outputs.jsonl`

One JSON object per line. Each line is a `JudgeOutput` record.

| Field | Type | Description |
|---|---|---|
| `instance_id` | `str` | Dataset instance identifier |
| `model_id` | `str` | Model that produced the inference output |
| `judge_name` | `str` | Display name of the judge |
| `score` | `int` | Quality score (1--5) |
| `verdict` | `str` | `"correct"`, `"incorrect"`, `"partial"`, or `"unknown"` |
| `reason` | `str` | Free-text explanation |
| `raw_output` | `str` | Raw judge response (for auditability) |
| `judge_prompt` | `str` | Full prompt sent to the judge |
| `timestamp` | `datetime` | When the evaluation was recorded (UTC) |
| `run_id` | `str` | Run identifier |

## `merged_results.parquet`

A single Parquet file joining inference outputs with judge evaluations. Generated by `merge-results`.

**Columns:**

| Column | Source |
|---|---|
| `instance_id` | InferenceOutput |
| `model_id` | InferenceOutput |
| `prompt` | InferenceOutput |
| `response` | InferenceOutput |
| `tokens_prompt` | InferenceOutput |
| `tokens_completion` | InferenceOutput |
| `latency_s` | InferenceOutput |
| `finish_reason` | InferenceOutput |
| `run_id` | InferenceOutput |
| `judge_<name>_score` | JudgeOutput (one column per judge) |
| `judge_<name>_verdict` | JudgeOutput (one column per judge) |
| `judge_<name>_reason` | JudgeOutput (one column per judge) |
| `avg_judge_score` | Computed: mean of all judge scores for this instance |
| `verification_score` | Computed: `1` if `avg_judge_score >= 3`, else `0` |

**Reading in Python:**

```python
import pandas as pd
df = pd.read_parquet("runs/adele_dev_run/merged_results.parquet")
```

## `run_manifest.json`

Run provenance metadata. Written at the start of inference, updated at the end.

| Field | Type | Description |
|---|---|---|
| `run_id` | `str` | Run identifier |
| `dataset_name` | `str` | Dataset name (e.g. `"adele"`) |
| `dataset_revision` | `str \| null` | Dataset revision hash (if available) |
| `model_id` | `str` | Model used for inference |
| `params` | `dict` | Inference parameters (temperature, max_tokens, etc.) |
| `start_time` | `datetime` | Run start time |
| `end_time` | `datetime \| null` | Run end time (`null` if still running or crashed) |
| `code_version` | `str` | Runner version |
| `total_instances` | `int` | Total instances to process |
| `completed_instances` | `int` | Instances completed so far |

## `metrics.json`

Summary statistics generated by `summarize`. Structure:

```json
{
  "per_judge": {
    "<judge_name>": {
      "n": 50,
      "mean_score": 3.8,
      "score_distribution": {"1": 2, "2": 5, "3": 10, "4": 20, "5": 13}
    }
  },
  "verification": {
    "n_instances": 50,
    "mean_avg_score": 3.6,
    "pass_rate": 0.76,
    "distribution": {"1": 5, "2": 8, "3": 12, "4": 15, "5": 10}
  },
  "inter_rater": {
    "<judge_a> vs <judge_b>": 0.65
  },
  "token_usage": {
    "<model_id>": {
      "prompt_tokens": 15000,
      "completion_tokens": 8000,
      "total_tokens": 23000
    }
  },
  "estimated_cost": {
    "<model_id>": 7.50
  }
}
```

The `estimated_cost` section only appears when `pricing.enabled` is `true` and pricing data is configured for the model.

## `ground_truths.json`

A cache of ground truth answers from the dataset:

```json
{
  "instance_001": "The answer is 42.",
  "instance_002": "Paris is the capital of France."
}
```

Written on first judge run, read on subsequent runs to avoid re-downloading the HuggingFace dataset. See [Resume & Idempotency](resume-and-idempotency.md#ground-truth-caching).
